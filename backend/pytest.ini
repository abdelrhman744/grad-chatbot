[pytest]
markers =
    real_model: uses a real local ML model (embeddings/cross-encoder/TrOCR) instead of a stub — no network needed (cached), but slower.
    live_qdrant: requires a live Qdrant (and usually MinIO) instance, e.g. via `docker compose up -d qdrant minio`.
